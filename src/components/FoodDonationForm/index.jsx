import React, { useState } from 'react';
import { Form, Input, InputNumber, Select, DatePicker, TimePicker, Checkbox, Radio, Switch, Button, Steps } from 'antd';

const { step } = Steps;
const { TextArea } = Input;
const { option } = Select;

function FoodDonationForm() {

    return(
        <div>
            <Form layout='horizontal'>

            </Form>
        </div>
    )
}

export default FoodDonationForm;